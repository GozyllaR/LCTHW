#include <stdio.h>

/* This is a comment. */
int main(int argc, char *argv[])
{
    int distance = 100;

    // this is also a comment
    printf("You are %d miles away.\n", distance);
    // Something more complex
    printf("This is %d complex stuff.\n", 1);

    return 0;
}
